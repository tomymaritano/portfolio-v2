/**
 * UI Components
 *
 * A minimal set of reusable UI components for the portfolio template.
 * All components use CSS Modules and follow the design system variables.
 *
 * Usage:
 * import { Button, Badge, Card, Input } from "@/components/ui";
 */

export { Button, type ButtonProps } from "./Button";
export { Badge, type BadgeProps } from "./Badge";
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
  type CardProps,
} from "./Card";
export { Input, type InputProps } from "./Input";
